// This file previously contained mock data.
// All data is now fetched from APIs via React Query hooks in src/hooks/api/.
// Re-export types for any remaining imports.
export type { DashboardStats, FinanceOverview } from '@/types/api.types';
